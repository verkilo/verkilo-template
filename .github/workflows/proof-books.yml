
name: Proof Books
on: [push]
jobs:
  build:
    if: "contains(toJSON(github.event.commits.*.message), 'proof')"
    name: Compile books
    runs-on: ubuntu-latest
    steps:
      - name: Get repo
        uses: actions/checkout@v1
      - name: Run Wordcount (Docker)
        uses: docker://merovex/wordcount:latest
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ACTION: compile
      - name: Upload artifacts
        uses: actions/upload-artifact@v1
        with:
          name: postal-marines-series
          path: build
